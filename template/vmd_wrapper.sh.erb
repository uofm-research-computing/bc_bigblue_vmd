#!/bin/bash

# This doesn't currently work
# Need to setup virtualgl correctly or something
# I do have x86_64 mesa library with llvmpipe though...

# Load the required environment
module load vmd virtualgl

module list

# Launch VMD
vglrun vmd
